import 'dart:convert';

class PublisherModel {
  String idPub;
  DateTime closedDate;
  DateTime foundingDate;
  String name;

  PublisherModel({
    this.idPub,
    this.closedDate,
    this.foundingDate,
    this.name,
  });
}
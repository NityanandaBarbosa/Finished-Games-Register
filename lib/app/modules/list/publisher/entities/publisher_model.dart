import 'dart:convert';

class PublisherModel {
  var idPub;
  var closedDate;
  var foundingDate;
  var name;

  PublisherModel({
    this.idPub,
    this.closedDate,
    this.foundingDate,
    this.name,
  });
}